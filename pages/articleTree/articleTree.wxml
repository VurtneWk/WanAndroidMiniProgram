<block wx:for="{{trees}}" wx:key="id">
  <view class='card'>
    <view class='title'>{{item.name}}</view>
    <block wx:for="{{item.children}}" wx:key="id">
      <text class='content'>{{item.name}} </text>
    </block>
  </view>
</block>